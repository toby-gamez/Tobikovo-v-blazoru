@page "/"

<PageTitle>Domů</PageTitle>
<link rel="stylesheet" href="IndexStyle.css">
        <section>
            <h1 class="citat">Učivo</h1>
            <h1 class="citat">základní školy</h1>
            <h1 class="citat">již</h1>
            <h1 class="citat">nikdy</h1>
            <h1 class="citat">nebylo</h1>
            <h1 class="citat">lehčí</h1>
            <h1 class="citat">získat.</h1>
            <h1 class="maly-text">Tobias Heneman</h1>
            <div style="margin-top: 25rem;">
                <h1 class="citat underlined">Učivo</h1>
            </div>

            <div class="card-grid">
                <ul class="grid">
                    <li class="grid-item">
                        <a href="mluvnice.html">
                            <h4 class="grid-h3">MLUVNICE</h4>
                        </a>
                    </li>
                    <li class="grid-item">
                        <a href="literatura.html">
                            <h4 class="grid-h3">LITERATURA</h4>
                        </a>
                    </li>
                    <li class="grid-item">
                        <a href="sloh.html">
                            <h4 class="grid-h3">SLOH</h4>
                        </a>
                    </li>
                    <li class="grid-item">
                        <a href="hudebni-vychova.html">
                            <h4 class="grid-h3">HUDEBNÍ VÝCHOVA</h4>
                        </a>
                    </li>
                    <li class="grid-item">
                        <a href="matematika.html">
                            <h4 class="grid-h3">MATEMATIKA</h4>
                        </a>
                    </li>
                    <li class="grid-item">
                        <a href="chemie.html">
                            <h4 class="grid-h3">CHEMIE</h4>
                        </a>
                    </li>
                </ul>
            </div>
        </section>
        <section>

            <div style="margin-top: 10rem;">

            </div>
            <h1>Konec</h1>
            <button onclick="Home()">Domů</button>
        </section>

    <script>
        function checkScroll() {
            const elements = document.querySelectorAll('h1, h2, h3, .card');
            const windowHeight = window.innerHeight;

            elements.forEach(el => {
                const elementTop = el.getBoundingClientRect().top;

                // Speciální pravidla pro různé typy elementů
                if (el.classList.contains('citat') || el.classList.contains('card')) {
                    if (elementTop < windowHeight * 0.90) {
                        el.classList.add('visible');
                    }
                } else if (el.classList.contains('maly-text')) {
                    if (elementTop < windowHeight * 0.9) {
                        el.classList.add('visible');
                    }
                } else {
                    // Výchozí chování pro ostatní elementy
                    if (elementTop < windowHeight * 0.10) {
                        el.classList.add('visible');
                    }
                }
            });
        }

        window.addEventListener('scroll', checkScroll);
        window.addEventListener('load', checkScroll);

        function Home() {
            window.location.href = "home";
        }
    </script>